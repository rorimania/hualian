const CONFIG = {
  // 后端服务地址
  // 微信开发者工具中需在「详情」→「本地设置」勾选「不校验合法域名」
  baseURL: 'http://localhost:3000',

  // API 密钥（与后端 .env 中的 API_KEY 保持一致）
  apiKey: 'hanakotoba_secret_key_2024',

  // 请求超时时间（毫秒）
  timeout: 10000,
}

module.exports = CONFIG
